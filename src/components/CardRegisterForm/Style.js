import styled from 'styled-components';

const CardRegisterForms = styled.form`
    .form-field {
        margin-top: 24px;
    }

    .form-title {
        margin-bottom: 8px;
    }

    .form-expire {
    }

    .form-expire-input {
        display: flex;
        justify-content: space-between;

        input {
            width: calc(50% - 4px);
        }
    }

    .form-birth {
    }
`;

export default CardRegisterForms;